{
  "$id": "calibration-record.schema.json",
  "title": "SARdine label calibration record",
  "description": "Class-conditional label noise rates per observer x method x class, with estimation provenance. Stored as {name}.sardine-calibration.json. The W025 trainer contract reads noise rates from records matched on (observer, method, class) — never from per-row confidence (confidence weighting soft-deletes rare classes). Unmatched labels at training time are an error, not silent weight=1.",
  "type": "object",
  "required": ["sardine:calibration", "id", "created", "estimated_by", "scope", "direction", "estimator", "classes", "rates", "estimation"],
  "properties": {
    "sardine:calibration": { "const": 1 },
    "id": { "type": "string" },
    "created": { "type": "string" },
    "estimated_by": {
      "type": "object",
      "required": ["kind", "ref"],
      "properties": {
        "kind": { "enum": ["human", "agent", "directive"] },
        "ref": { "type": "string" }
      }
    },
    "scope": {
      "type": "object",
      "required": ["observer", "method"],
      "properties": {
        "observer": { "enum": ["human", "ai", "insitu", "model"] },
        "method": { "type": "string" },
        "region": { "type": "string" },
        "campaign": { "type": "string" }
      }
    },
    "direction": {
      "enum": ["true_given_label", "label_given_true"],
      "description": "Which conditional the rows state. Gauge-audit precision estimates: true_given_label. Adjudication tallies of a proposer: label_given_true."
    },
    "estimator": {
      "enum": ["audit", "adjudication"],
      "description": "How the rates were obtained. adjudication-derived rates are SAR-correlated (analyst saw the imagery) and NOT directly usable as training weights; the trainer MUST NOT average across estimator values — agreement between the two is a reportable result, a silent mean is a bug."
    },
    "classes": { "type": "array", "items": { "type": "string" }, "minItems": 1 },
    "rates": {
      "type": "array",
      "minItems": 1,
      "items": {
        "type": "object",
        "required": ["given", "outcome", "rate", "n"],
        "properties": {
          "given": { "type": "string" },
          "outcome": { "type": "string" },
          "rate": { "type": "number", "minimum": 0, "maximum": 1 },
          "n": { "type": "integer", "minimum": 1 },
          "ci95": { "type": "array", "items": { "type": "number" }, "minItems": 2, "maxItems": 2 }
        }
      }
    },
    "estimation": {
      "type": "object",
      "required": ["procedure", "data_ref", "date"],
      "properties": {
        "procedure": { "type": "string" },
        "data_ref": { "type": "string" },
        "date": { "type": "string" }
      }
    }
  }
}
